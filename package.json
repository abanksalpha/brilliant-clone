{
  "name": "physics-arcade",
  "version": "0.0.0",
  "private": true,
  "description": "Physics Arcade workspace",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "test": "npm --prefix frontend run test"
  }
}
